# Contratos

Lo que se acordó. Se versiona; no se reescribe para que coincida con el código.

| Ruta | Qué es |
|---|---|
| [`specs/`](specs/) | Specs por módulo y material del cliente de M01/M02 |
| [`db/`](db/) | Schema de diseño por módulo — **numeración interna, no la del cliente** |
| [`mvp-scope.csv`](mvp-scope.csv) | Alcance MVP. Una columna en el schema no es una feature |

Si el código y el contrato no coinciden, el índice del módulo (`context/moduloNN-*/`)
debe decirlo. No “arreglar” el Excel ni el `.docx`.

Los entregables de M04–M07 viven en `documentacion/`, fuera de este repo.
