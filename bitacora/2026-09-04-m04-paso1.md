# M04 · Paso 1 (Carga) — estado al 2026-09-04

Paso 1 = el onboarding completo: Carga → Analizando → Revisión → Resultado.
Termina en la anotación `5897:12851` ("Usuario continúa al paso 2").

## Mergeado en qa

- PR #130, #131, #132 (front) · #236, #237 (back). 26 commits del módulo.
- 13 endpoints en uso, todos existentes. Tres 404 del draft corregidos:
  `DELETE /debts/:id`, `POST /debts/:id/reject`, `POST /debts/route/payments`.
- Infra compartida con M01: `utils/statementFile.ts`, `components/StatementDocRow.tsx`,
  `hooks/useStatementUploadQueue.ts`, `constants/deudaStyles.ts`.
- Reglas devueltas al backend: punto de entrada (`entry.surface`) y catálogo de tipos.

## Esperando review

- **front #133** — tokens + heurística de Resultado + el 1/15 de M01-JIRA-002.
  Toca M01: necesita pasada por QA (subir 3+ cartolas, salir, reingresar).

## Cola pendiente, en orden

1. **Migrar M01 al código compartido.** `OnboardingDocScreen` → `StatementDocRow`;
   `OnboardingAnalyzingScreen` → `useStatementUploadQueue`. 1.815 líneas, PR aparte,
   requiere QA con sesión y archivos reales. **Esperar a que #133 esté validado**:
   las dos cosas tocan `OnboardingAnalyzingScreen`.
2. **Seguimiento de los bloqueos de backend:** back#234 ("Última cuota pagada"
   necesita columna o cambio de diseño) y back#235 (imports colgados en `processing`).
3. **Correo a diseño y PMO** con los bloqueantes de diseño.

## Bloqueado por diseño (contenido del correo)

1. Frames de **Analizando**, **Revisión** y **Resultado** — 3 pantallas implementadas
   contra ningún frame.
2. **Estados por documento** durante la subida ("Subiendo/Procesando/Documento leído/
   No pudimos leer"). Son por documento, pero la pantalla modela la espera como cinco
   pasos globales. Falta decidir cuál gana.
3. **"Confirmar después"** en Revisión: hoy es un botón muerto (`onPress={() => {}}`).
4. **Regla de "Continuar"**: la pantalla habilita con ≥1 confirmada, el backend
   (`canViewResult`) exige que no quede ninguna por confirmar, y la caja informativa
   promete que las pendientes quedan para después. Las tres no pueden ser ciertas.
5. **"Actualización de datos"**: el CTA que la nota pide en Resultado, Resumen y Avance
   no está en ningún frame.
6. **Repositorio de pendientes**: mencionado en la nota como no diseñado.
7. **Coral inconsistente**: Analizando usa `#EE8D78`, Revisión y Resultado `#EF9682`.

Todo está también en KabeliDev/front-walvy#116, con el diagrama del flujo.

## Recordatorios de método

- Verificar contra **`qa`**, no `develop`. `git remote prune origin` antes de razonar
  sobre ramas: leí un `origin/develop` borrado durante todo el módulo.
- Antes de escribir una regla en el front, buscarla en el backend. Pasó cuatro veces:
  reglas de archivo, cola de subida, punto de entrada, heurística de presión.
