---
description: Diseñar o implementar el asistente IA (M07 del cliente).
---

El M07 del cliente es Agente IA. En `contratos/db/` es `modulo8.md`. No hay carpeta de módulo todavía; entregables en `documentacion/`.
Código hoy: módulo Nest `ai` sin endpoints. El diseño viejo de «Sprint 8» no es estado.

Leé `context/wiki-codigo/` y el contrato del cliente en `documentacion/` antes de proponer schema o prompts.

Prioridad: contexto financiero rico (diagnóstico, deudas, leaks) sobre UI brillante.
Tono: orientativo, español, CLP, sin asesoría certificada.
No inventes tablas que las entities no tengan. Si proponés schema, contrastalo con el código.
